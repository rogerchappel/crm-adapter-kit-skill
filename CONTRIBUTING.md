# Contributing

Keep CRM adapter changes local-first, fixture-backed, and reviewable. New
validation behavior should include a fixture or test that shows the expected
approval packet.

Before opening a pull request, run:

```sh
npm run release:check
```
